<div class="bg-light p-5 rounded-lg m-3">
  <h1 class="display-4">Bem-vindo ao Blog do Node</h1>
  <p class="lead">Este é um Blog simples, desenvolvido com <strong>Node.js</strong> + <strong>Express</strong> + <strong>MongoDB</strong>.</p>
  <hr class="my-4">
  <p>Aqui você pode ler sobre as novidades do mundo da computação.</p>
  <a class="btn btn-primary btn-lg" href="/usuarios/login" role="button">Crie uma conta</a>
</div>

<hr class="m-4">
<h2 class="container">Postagens recentes: </h2>

{{#each postagens}}
<div class="card my-4">
    <div class="card-body mt-2">
        <h3>{{titulo}}</h3>
        <p>{{descricao}}</p>
        <a href="/postagem/{{slug}}"><button class="btn btn-success">Leia mais</button></a>
        <hr>
        <small>Categoria: {{categoria.nome}}</small><br>
        <small>Data de criação: {{data}}</small>
    </div>
</div>
{{else}}
<h5>Nenhuma postagem</h5>
{{/each}}

